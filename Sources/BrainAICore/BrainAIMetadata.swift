import Foundation

/// Product version for bundles, DMG filenames, and protocol handshakes (MCP, etc.). Bump on release.
public enum BrainAIMetadata {
    public static let marketingVersion = "0.1.5"
}
